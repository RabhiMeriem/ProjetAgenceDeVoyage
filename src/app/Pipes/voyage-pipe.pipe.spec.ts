import { VoyagePipePipe } from './voyage-pipe.pipe';

describe('VoyagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new VoyagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
